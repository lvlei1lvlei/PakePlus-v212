[package]
name = "tauri-mobile-scanner"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0-beta", features = ["mobile"] }

[target."cfg(any(target_os = \"android\", target_os = \"ios\"))".dependencies]
tauri-plugin-barcode-scanner = "2.0.0-beta.0"